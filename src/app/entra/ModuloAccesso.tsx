// Spostato in src/components/ModuloAccesso.tsx (ora condiviso con l'ingresso).
// Questo file non si può cancellare da qui per un blocco del filesystem locale
// (vedi le note su OneDrive nel resto del progetto) — puoi eliminarlo tu a mano.
export { default } from "@/components/ModuloAccesso";
